import React from 'react';
import {View} from 'react-native';

export const Hr = () => {
  return (
    <View
      // eslint-disable-next-line react-native/no-inline-styles
      style={{
        marginTop: 30,
        borderBottomColor: '#b8b6a2',
        borderBottomWidth: 1,
      }}
    />
  );
};
